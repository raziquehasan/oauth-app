server:
  port: 8083

spring:
  application:
    name: auth-app-backend

  # =======================
  # 🗄️ DATABASE
  # =======================
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app_yt
    username: root
    password: razique@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  # =======================
  # 🔐 OAUTH2 CONFIG
  # =======================
  security:
    oauth2:
      client:
        registration:

          google:
            client-id: 368385459014-fbf36sr7gm64e9v4bo8emqg538sqvg0s.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
              - openid
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-name: Google

          github:
            client-id: Ov23liphJy1tUMfS4RL5
            client-secret: 1fb0227787e23681b6fd0969a4090eabce72801f
            scope:
              - read:user
              - user:email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-name: GitHub

        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

    # =======================
    # 🔐 JWT CONFIG (MATCHES JwtService)
    # =======================
    jwt:
      secret: THIS_IS_A_VERY_LONG_AND_SECURE_JWT_SECRET_KEY_FOR_DEV_ENVIRONMENT_1234567890
      issuer: auth-app-backend

      access-ttl-seconds: 900        # 15 minutes
      refresh-ttl-seconds: 604800    # 7 days

      refresh-token-cookie-name: refresh_token
      cookie-http-only: true
      cookie-secure: false
      cookie-same-site: Lax
      cookie-domain: localhost

# =======================
# 🌐 CORS
# =======================
app:
  cors:
    front-end-url: http://localhost:3000

  auth:
    frontend:
      success-redirect: http://localhost:3000/login/success

# =======================
# 🪵 LOGGING
# =======================
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: INFO
